<?php 

// Per stampare tipo e contenuto di un expr, utile nella fase di debug, devo usare var_dump(<expr>)

$a = array(1, array("a", "b", "c"));

var_dump($a); // print type and content of $a (array)